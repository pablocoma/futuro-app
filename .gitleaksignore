# Hallazgos concretos aceptados, por huella exacta.
#
# La huella es commit:fichero:regla:linea, así que cada línea de este fichero
# excusa UN hallazgo y nada más: ni la regla en general, ni el fichero, ni
# ese patrón en otro sitio. Si mañana apareciera un secreto de verdad en ese
# mismo fichero, el job seguiría saltando. Es la garantía que hace aceptable
# tener excepciones, porque el job de `fugas de datos` es lo único que
# comprueba el primer principio del repositorio.
#
# Se usa este mecanismo y no un .gitleaks.toml con allowlists porque la
# sintaxis de `[[allowlists]]` con `condition`/`regexTarget` necesita una
# version de gitleaks mas nueva que la 8.24.3 que trae gitleaks-action@v2:
# alli se ignora en silencio y el PR se bloquea igual. Esto funciona en
# ambas.

# `http://api:8000/api/offers/ingest` en un test del cliente del frontend.
# `generic-api-key` lee lo que sigue a `api:` como si fuera una credencial;
# es el nombre del servicio en docker-compose.yml y su puerto, una URL que
# solo resuelve dentro de la red de Docker.
fbed4ce792dfa594832db4247eee398b809340b6:services/web/src/lib/api.test.ts:generic-api-key:99
